{
  "name": "@su-record/vibe",
  "version": "4.0.0-alpha.0",
  "description": "vibe 4 — an AX/FDE harness: scenarios you approve once, checks the harness runs itself, human tokens for irreversible actions.",
  "type": "module",
  "bin": {
    "vibe": "dist/cli.js"
  },
  "files": [
    "dist/",
    "skills/",
    "hooks/",
    "card.md",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.10",
    "tsx": "^4.23.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "license": "MIT",
  "repository": "https://github.com/su-record/vibe",
  "keywords": [
    "harness",
    "fde",
    "verification",
    "claude-code",
    "codex",
    "chatgpt"
  ]
}
