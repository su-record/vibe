# Pre-registered claim — 4.1.23, the fair bench, 2026-09-09

Written before the bench runs; not adjusted after seeing the data. This is the first run in which the `on` arm cannot read the key: each task is split into `checks/` (copied to both arms), `key/` (the reference answer, the expected output, the fake user — reaches the workspace only at judge time) and `judge/` (an intent with no key in it), proved by `checks/bench-no-key.js` and the key-value grep in `checks/bench-judge.js`. Both arms get identical files; the `on` arm adds `.vibe/` with the intent and scenarios, the card, the skills and the hook. Also new since the fifth run: results bound to the tree, approval re-verified at check, parents judged by this run, every command segment gated; queries never repair the install. Arms: claude × {on, off}, codex × {on, off}; five runs per arm per task; `--parallel 3`; both arms isolated.

- Overhead set (settlement, vibe-fix, report): `on` checks not worse; `on` weighted input tokens ≤ `off` × 1.25; `on` ms ≤ `off` × 1.5 — per client, means over five runs. Turns reported.
- Direction set: `ask` — per client, `on` scores higher on checks than `off`; `session-split` — per client, `on` checks not worse and `on` weighted tokens over the two sessions ≤ `off`.
- Context set (brownfield): `on` weighted tokens ≤ `off` × 0.7 per client.
- The gate is a release condition (`gate-is-release` in the intent): the release ships only if every rule passes; otherwise README says which rule fails and the release waits. Whatever falls: the numbers are reported as measured, and every earlier `on`-arm number stays withdrawn.

## During the run — Codex out of credit
At 08:5x the Codex account hit its usage limit ("You've hit your usage limit … try again at Sep 15th, 2026"): every codex `off` run and the codex `on` brownfield runs ended in seconds with nothing done. Those 24 lines are annotated `error` in the ledger and the gate never counts a line with an error; the arm was stopped. Codex `on` settlement, vibe-fix, report, ask and session-split ran before the limit and stand, but without an `off` arm they compare to nothing. This release measures Claude Code; Codex is unmeasured until the account can run again, and README says so. `bench/run.js` now names a client's limit or capacity error on the line and stops the arm on it.

## Result (2026-09-09, 120 valid runs; Codex `off` and `on` brownfield rerun after the credit came back)
Overhead set, `on` vs `off` — weighted tokens (ratio) · seconds (ratio) · turns:
- claude settlement 31,966 vs 38,204 (**0.84**) · 46 s vs 80 s (0.58) · 7.4 vs 7.8 — within; the bare model spends more here
- claude vibe-fix 23,152 vs 19,337 (**1.20**) · 19 s vs 15 s (1.23) · 5.0 vs 3.0 — within
- claude report 28,681 vs 19,804 (**1.45**) · 26 s vs 19 s (1.37) · 6.2 vs 3.2 — **over ×1.25 on tokens**
- codex settlement 103,136 vs 105,894 (**0.97**) · 67 s vs 62 s (1.08) · 11.0 vs 9.2 — within
- codex vibe-fix 60,968 vs 61,826 (**0.99**) · 24 s vs 23 s (1.07) · 7.0 vs 6.8 — within
- codex report 65,089 vs 61,105 (**1.07**) · 28 s vs 26 s (1.09) · 7.4 vs 6.0 — within
Quality: every arm 100% on every overhead task.

Direction set:
- `ask` — claude **separates**: `on` 3/3 in five of five (two or three questions each), `off` 0/3 in five of five — the bare model never asked and quoted in USD every time; and `on` did it for fewer tokens (0.67) in less time (0.63). codex does **not** separate: `on` 3/3 ×5, `off` 3/3 ×5 — the bare Codex asked once per run and got the terms; `on` spent 1.17× the tokens for it.
- `session-split` — claude **holds**: 54,116 vs 67,499 (0.80), 10.4 vs 12.2 turns; codex 249,631 vs 208,975 (1.19), 21.5 vs 17.2 — falls (four valid `on` runs; one hit the limit).

Context set (brownfield): claude 101,823 vs 117,505 (**0.87**), 190 s vs 184 s; codex 694,286 vs 970,417 (**0.72**), 210 s vs 297 s — both under `off`, neither under 0.7.

Gate verdict: **fails** — claude report tokens 1.45 (rule 1.25), brownfield 0.87 / 0.72 (rule 0.7), codex `ask` does not separate. Everything else passes. Compared with the contaminated fifth run the fair `on` arm is *better*, not worse: with no key to read, settlement on claude went from 1.02 to 0.84, brownfield on codex from 1.93 to 0.72, and `ask` on claude from a claimed separation to a measured one.

## After the cause was fixed (2026-09-09 10:05, Claude `on` rerun on every set)
The fair run's transcripts said the claude `on` arm still ran `vibe state` as its first turn: the session-start hand-over existed only in the plugin path, not in the settings hook file the bench (and any plugin-less install) uses. With `notify.js session` on SessionStart, the hand-over fired in every run and the model ran no `vibe state` at all. Claude `on` vs `off`, tokens (ratio) · seconds (ratio) · turns:
- settlement 27,477 vs 38,204 (**0.72**) · 36 s vs 80 s (0.45) · 4.8 vs 7.8
- vibe-fix 22,639 vs 19,337 (**1.17**) · 18 s vs 15 s (1.15) · 4.0 vs 3.0
- report 24,415 vs 19,804 (**1.23**) · 22 s vs 19 s (1.15) · 4.4 vs 3.2 — within ×1.25 now
- ask 108,008 vs 154,368 (0.70) · 185 s vs 309 s (0.60) · `on` 3/3 ×5, `off` 0/3 ×5 — separates
- session-split 41,963 vs 67,499 (**0.62**) · 58 s vs 84 s (0.69) · 6.0 vs 12.2 turns — holds
- brownfield 86,195 vs 117,505 (**0.73**) · 113 s vs 184 s (0.61) · 14.0 vs 20.2
Every Claude Code overhead and direction rule passes. Gate still fails on two: the context rule (claude 0.73, codex 0.72 against 0.7 — a target with no mechanism behind it on the default path: `vibe map` and `vibe context` are never called there) and `ask` on Codex, where the bare model asks on its own (nothing to prevent). Both are decisions about the rules, not fixes in vibe; recorded here before any rule changes.
