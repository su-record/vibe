# Pre-registered claim — 4.1.24, 2026-09-09 (KST night)

Written before the bench runs; not adjusted after seeing the data. Same fair bench and rules as 4.1.23's final run, plus one rule: on a trap task, `on` turns at most 2× `off` turns per client — the number 4.1.23 left open (Codex 49.8 vs 16.2 on `ask`). What changed: asking is a stop (`vibe ask` says stop and wait; the `next` line says wait while unanswered and carries the answer once answered); a failed check names its files and the procedure says fix what the check names, `vibe context` only when that is not enough; the Stop hook names a completion claim the verdict does not support; `vibe status` says whether Codex trusts vibe's hooks; the scope skill probes before scenarios and writes the check first. Only the `on` arms rerun (nothing here touches the bare arm; the latest five `off` runs per task stand).

- Overhead set: `on` checks not worse; `on` weighted tokens ≤ `off` × 1.25; `on` ms ≤ `off` × 1.5 — per client.
- Direction set: `ask` separates on at least one client and `on` is not worse on the others; `on` turns ≤ `off` × 2 per client; `session-split` not worse on checks and no more tokens over its two sessions.
- Context set (brownfield): `on` tokens ≤ `off` × 0.7 per client.
- The gate is the release condition; whatever falls is reported as measured and the release waits.

## First rerun (2026-09-09 22:13–22:31 KST, `on` arms)
- Codex `ask`: 29.0 vs 16.2 turns (1.79×, within the new 2× rule; was 49.8); quality 3/3 both arms.
- Claude `ask`: `on` 0, 3, 3, 3, 1 vs `off` 0 ×5 — separates, but two `on` runs fell short for a bench reason, not a harness one: both asked a legitimate question the fake user did not answer (one about digit formatting, one about `key/expected.json`, which `checks/verify.cjs` named) and then waited, as told. The fake user now answers any question about the quote with the whole of the terms, format included, and the verify scripts no longer name a key file: the judge supplies the terms through `VIBE_KEY_EXPECTED` when it runs, and on disk the script says it is judge-time. `ask` reruns.
- Overhead and session-split: within on both clients (claude 0.72 / 1.19 / 1.21, 0.69; codex 0.80 / 1.01 / 0.85, 0.81).
- brownfield: claude 0.65; codex **0.75** against 0.7, where the previous run measured 0.63 with nothing in this release touching that path — the spread of five Codex runs straddles the rule (one run polled `npm test` output through four `write_stdin` waits). Recorded; a methodology decision, not a threshold move.
