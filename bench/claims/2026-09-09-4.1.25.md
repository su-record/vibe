# Pre-registered claim — 4.1.25, 2026-09-09 (KST night)

Written before the bench runs; not adjusted after seeing the data. New this run: a third arm, `scoped` — the card, skills and hooks with no intent; vibe runs discover and scope itself from the brief and approves under `tokens off`; the judge's scenarios stay hidden until judge time. Two tasks: `anomaly` (a repeated order id, a refund as a negative amount, a euro row; `docs/finance.md` holds the rules; `vibe profile` names all three) and `handover` (`session-split` with the first session on Claude Code and the second on Codex). The `on` and `off` arms of the other tasks stand from 4.1.24 (nothing here touches them); `scoped` runs on every task, `anomaly` on every arm, `handover` on every arm with the client pair.

- Overhead and context sets: `on` as before; `scoped` checks not worse than `off`, tokens reported.
- Direction set: `anomaly` separates when `on` or `scoped` scores higher on checks than `off` on at least one client, not worse on the others, `on` turns ≤ 2× `off`; `handover` and `session-split` hold when `on` is not worse and spends no more tokens.
- What is expected and not claimed: bare frontier models read the docs of a small workspace, so `anomaly` may not separate; what the run will say either way is what scoping costs (tokens, turns) and how many scenarios and checks the agent writes for itself.
- The gate is the release condition; whatever falls is reported as measured and the release waits.
