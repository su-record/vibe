# Pre-registered claim — 4.1.22, fourth run, 2026-09-09

Written before the bench runs; not adjusted after seeing the data. Same arms and rules as the third run; the overhead set and `session-split` are rerun and appended to the same ledger (the gate reads the latest five runs per arm and task; brownfield stands on the third run — nothing here touches the map). What changed since the third run, from its transcripts: the model loaded `vibe-build` on every task because its description began "Build", and at DONE loaded `vibe-handoff` and spent five to seven turns reading evidence and code to write a report. Now the three stage skills gate themselves in their descriptions (build for a `size: full` task only; prove only on STUCK; handoff only when the intent asks for a document) and the DONE `next` line says: answer from this output, no skill, no further reads.

- Overhead set: `on` checks not worse; `on` turns ≤ `off` + 2; `on` ms ≤ `off` × 1.5 — per client, means over five runs.
- Direction set (`session-split`): `on` checks not worse and `on` turns summed over the two sessions ≤ `off` summed, per client.
- Whatever falls: the numbers are reported as measured; if +2 still fails, README's promise is reworded to the measured number and no further cut is made in this release.

## Result (2026-09-09, 80 runs appended; the gate reads the latest five per arm)
Overhead set, `on` vs `off` (mean turns · mean seconds; the third run in brackets):
- claude settlement 9.6 vs 3.8 [9.4 vs 5.6] · 53 s vs 44 s — **over +2** (five scenarios: `size: full`, the build skill loads and prompts `vibe profile`, `skill list`, `state --graph`)
- claude vibe-fix 5.8 vs 3.2 [7.6 vs 3.4] · 20 s vs 14 s — over +2 by 0.6
- claude report 5.6 vs 3.0 [8.8 vs 3.0] · 22 s vs 17 s — over +2 by 0.6
- codex settlement 14.0 vs 6.2 [17.0 vs 7.4] · 90 s vs 58 s — over; one `on` run scored 0/5 in 40 s with `usage: missing`: the session log says "Selected model is at capacity" (server_overloaded), not the harness
- codex vibe-fix 7.0 vs 7.2 [10.8 vs 8.0] · 33 s vs 26 s — **within**
- codex report 6.8 vs 6.0 [9.2 vs 6.0] · 50 s vs 30 s — within on turns, over ×1.5 on time
Quality: every arm 100% on every overhead task except the overloaded codex run.

Direction set (`session-split`): claude `on` 10.4 vs `off` 10.4 turns, 50,092 vs 57,316 weighted tokens, $0.45 vs $0.51 — **holds on claude** (same turns, 12% fewer tokens); codex 20.6 vs 15.6 — falls.

Reading: the fourth cut took claude's report and vibe-fix from +5.8 to +2.6 and session-split from +8.0 to 0; settlement did not move because its five scenarios make it `size: full` and the build skill loads. The +2 rule is not met on claude (by 0.6 on the small tasks, 5.8 on the full one) and, per this pre-registration, no further cut is made in 4.1.22: the README states the measured cost, and the gate keeps +2 as the next release's target. What the next cut is, named now: `size: full` should mean something in the build skill applies — an irreversible scenario, a review/http/eval check, or more scenarios than one agent should build — not a count of five.
