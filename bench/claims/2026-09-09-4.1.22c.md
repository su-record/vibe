# Pre-registered claim — 4.1.22, third run, 2026-09-09

Written before the bench runs; not adjusted after seeing the data. Same arms and rules as the second run. What changed since it: `vibe state` names each scenario's check beside its `then` (the state is the brief), `vibe check` ends with the `next` line, and the card starts at `vibe state` and loads only the skill `next` names — the four reading turns the second run's transcripts named. `irreversible-trap` is retired; the direction set is `session-split` alone.

- Overhead set: `on` checks not worse; `on` turns ≤ `off` + 2; `on` ms ≤ `off` × 1.5 — per client, means over five runs.
- Direction set (`session-split`): `on` checks not worse and `on` turns summed over the two sessions ≤ `off` summed, per client.
- Context set (brownfield): the 0.7 rule stays pre-registered and is expected to fall; the claim is `on` weighted tokens ≤ `off` on both clients at equal quality.
- Whatever falls: the numbers are reported as measured. If the overhead set still fails +2, the release ships with the number as measured and the claim in README is reworded to it.

## Result (2026-09-09, 100 runs, both arms isolated, `usage: missing` on none)
Overhead set, `on` vs `off` (mean turns · mean seconds; the second run in brackets):
- claude settlement 9.4 vs 5.6 [9.2 vs 4.6] · 62 s vs 59 s — **over +2**
- claude vibe-fix 7.6 vs 3.4 [7.2 vs 3.0] · 23 s vs 18 s — **over +2**
- claude report 8.8 vs 3.0 [9.0 vs 3.0] · 34 s vs 20 s — **over +2**
- codex settlement 17.0 vs 7.4 [14.6 vs 7.8] · 96 s vs 66 s — over +2 (turns)
- codex vibe-fix 10.8 vs 8.0 [10.2 vs 7.4] · 39 s vs 36 s — within
- codex report 9.2 vs 6.0 [7.2 vs 6.0] · 46 s vs 37 s — over +2 (turns)
Quality: every arm 100% on every overhead task. The state-as-brief cut moved nothing on turns.

Direction set (`session-split`): quality 4/4 everywhere; claude `on` 17.2 vs `off` 9.2, codex 26.4 vs 15.2 — **falsified again**.

Context set (brownfield): quality equal (codex `off` one run 4/5). Weighted tokens claude `on` 89,221 vs `off` 163,465 (**−45%**, 168 s vs 356 s, $0.72 vs $1.19) — the state-as-brief did reach the one task with something to rediscover; codex `on` 795,517 vs `off` 533,352 (+49%). The 0.7 rule holds on claude for the first time and falls on codex; the claim as pre-registered (both clients) falls.

What the transcripts say (claude, `on`): every task loaded `vibe-build` after `vibe state` because the skill's description began "Build"; at DONE (`session-split`'s second session) the model loaded `vibe-handoff` and spent five to seven turns on `vibe state --json`, `vibe evidence`, `cat ledger.cjs` and the tests to write a report. That is the fourth cut (`2026-09-09-4.1.22d.md`): the stage skills gate themselves, DONE answers from the check output.
