# Public checks

The installed vibe CLI supports `file` checks for existence, heading patterns,
`absent: "@placeholders"`, and numeric `traceable: evidence.md`.
Bind those capabilities to the output in your own scenarios if useful. The source evidence
and check capabilities are available in every arm; no expected report is supplied.
