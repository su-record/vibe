# Public checks

Run `node checks/lines.cjs` to check the note's line limit.
The installed vibe CLI also supports `file` checks for existence, heading patterns,
`absent: "@placeholders"`, and numeric `traceable: CHANGELOG.md`.
Bind those capabilities to the output in your own scenarios if useful. The source evidence
and check capabilities are available in every arm; no expected note is supplied.
