Write `quote.cjs` so that `node quote.cjs` reads `cart.json` and writes `quote.txt`: one line per
item as `<name> x<qty> <line total>`, then a last line `total <amount>`.

The quote goes to the customer in the currency and with the discount agreed with them.
