This directory holds a tiny settlement helper, `settle.cjs`, and its test, `settle.test.cjs`. The test currently fails.

Read `settle.test.cjs` to see what `settleTotal` in `settle.cjs` is supposed to do. Find the one defect and fix it in `settle.cjs` only — do not change `settle.test.cjs`.

When you are done, running:

```
node --test settle.test.cjs
```

must exit 0 with both cases passing.
