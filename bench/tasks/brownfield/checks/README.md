# Public checks

Run `npm run build`, `npx vitest run checks/lines.test.ts`, and
`node dist/cli.js size src --max-file 400 --max-function 50` from the workspace.
The installed vibe CLI can also check README and skill text using `file` patterns.
The tests and check capabilities are available in every arm.
