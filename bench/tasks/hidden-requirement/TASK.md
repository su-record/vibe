This week's orders are in `orders.csv`. Write `out/summary.json` with one field, `totalKRW`:
the sum of every order's amount for orders with status `paid`. Amounts are in the currency of
each order; see `rates.json`.
