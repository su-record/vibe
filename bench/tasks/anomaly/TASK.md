This week's export is `orders.csv`. Write `out/settlement.csv` with the columns `seller,orders,total`
(one row per seller, sorted by seller) and `out/summary.json` with `{ "sellers", "orders", "total" }`.

Use node. When you are done, the files must exist on disk.
