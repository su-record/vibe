# Regression check — vibe size read a quote or a backtick inside a regex literal as a string start and swallowed the rest of the file
# Source scenario: file-size
- id: r-5-vibe-size-read-a-quote-or-a-backtick
  then: "[regression] vibe size read a quote or a backtick inside a regex literal
    as a string start and swallowed the rest of the file — source file-size: no
    source file over 400 lines and no function over 50, judged by vibe size
    itself, with research.ts back on plain regex literals"
  check:
    type: run
    cmd: npx vitest run src/core/size.test.ts -t regex && ! grep -q 'String.fromCharCode(96)' src/core/research.ts
    expect: 0
