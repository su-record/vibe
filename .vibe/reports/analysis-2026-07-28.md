# 프로젝트 분석 — @su-record/vibe v3.2.13

> ⚠️ **이후 변경 (v3.2.22, 2026-08-09)**: 이 문서가 언급하는 `ReviewRace.ts` · `SkillRepository.ts` 는 참조 0건으로 확인돼 삭제됐다. 아래 본문은 **작성 시점의 사실**이며 수정하지 않는다 — 당시 판단의 근거를 남기기 위함이다.


- **모드**: Mode 4 (Project Quality — code + deps + arch)
- **대상**: `/home/ubuntu/repos/vibe` (main @ `ce4de17`)
- **실행일**: 2026-07-28
- **깊이**: L3 (구조 + 로직/데이터 흐름 + 의존성)

---

## 1. 요약

| 축 | 판정 | 근거 |
|---|---|---|
| 빌드 / 타입 | ✅ GOOD | `tsc --noEmit` exit 0, `strict: true` |
| 테스트 | ✅ GOOD | 83 파일 / 1557 테스트 전부 통과 (17.15s) |
| 아키텍처 | ✅ GOOD | 순환 의존 0, 레이어 위반 0 (type-only 2건 제외) |
| 자체 SSOT 게이트 | ✅ GOOD | 4개 validate 스크립트 전부 FRESH/PASS |
| 코드 품질 | ⚠️ WARNING | 프로덕션 `any` 30건, lib 계층 `console.log` 9건 |
| 테스트 배치 | ⚠️ WARNING | 직접 테스트 없는 모듈 155/196 (79%) |
| 의존성 | ⚠️ WARNING | 취약점 5건(high 3) · 메이저 낙후 7개 |

**종합: GOOD (90/100)** — 뼈대(타입·레이어·게이트)는 견고. 리스크는 전부 주변부(의존성 갱신, 국소적 `any`, hooks 계층 테스트 밀도)에 몰려 있다.

---

## 2. 규모

| 영역 | 파일 | LOC | 테스트 |
|---|---|---|---|
| `src/` (TS) | 290 | 60,606 | 83 파일 / 1557 케이스 |
| `hooks/scripts/` (JS) | 50 | 11,263 | 23 파일 |
| `skills/` | 52 SKILL.md | — | `validate:skill-invocation` |
| `agents/` | 11 (전역 7 + 조건부 4) | — | `sync:agent-models:check` |
| `languages/` | 23 스택 프로파일 | — | — |
| `vibe/` (배송 SSOT) | 53 | — | — |

---

## 3. 아키텍처

### 3.1 레이어 구조

```
src/cli/      ── 진입점·명령·postinstall·setup   (infra 40회, tools 23회 참조)
src/tools/    ── MCP 툴 (memory/convention/spec/ui/loop/…)  (infra 108회 참조)
src/infra/    ── 라이브러리·타입 (하위 레이어)
```

- **순환 의존 0** — 230개 프로덕션 모듈 대상 DFS 스캔 결과 사이클 없음.
- **역방향 참조 2건, 모두 `import type`** — 런타임 결합 아님:
  - `src/infra/lib/codex-proxy.ts:16` → `cli/types.js`
  - `src/infra/lib/config/GlobalConfigManager.ts:11` → `cli/types.js`
  - 개선하려면 `CodexProxyConfig` / `GlobalVibeConfig` 를 `src/infra/types/` 로 내리면 방향성이 완전히 단방향이 된다. (P3)
- `tools → cli` 참조는 테스트 파일 1개(`specGenerator.test.ts`)뿐 — 프로덕션 경로 위반 없음.

### 3.2 SSOT 자기검증 체계

이 저장소의 가장 특징적인 강점. 문서·상수·에셋 개수가 갈라지는 것을 코드로 막는다.

| 스크립트 | 결과 |
|---|---|
| `gen:skill-docs:check` | FRESH — SKILL-CATALOG.md 최신 |
| `sync:agent-models:check` | FRESH — 11개 에이전트 모델 섹션 동기 |
| `validate:skill-invocation` | ✓ 52 skills 선언 유효 |
| `validate:counts` | ✓ README/package.json 개수 주장(52 skills, 7+ agents) 실제와 일치 |

### 3.3 CI

- `.github/workflows/test.yml` — push 시 `pnpm build` + `pnpm test`
- `.github/workflows/release.yml` — 태그 시 build → test → release-notes 생성 → `npm publish --provenance` → GH Release
- 릴리스 전 테스트 게이트 존재 ✅

---

## 4. 코드 품질

### 4.1 타입 안정성

`strict: true`, `tsc --noEmit` 클린. 프로덕션 코드의 `any`는 **30건**이며 한 곳에 몰려 있다:

| 파일 | 건수 | 성격 |
|---|---|---|
| `src/infra/lib/figma/extract.ts` | 13 | Figma REST 원본 노드 |
| `src/tools/memory/getMemoryGraph.ts` | 3 | — |
| `src/infra/lib/figma/audit.ts` | 2 | Figma 원본 노드 |
| `src/tools/convention/validateCodeQuality.ts` | 2 | — |
| `src/cli/detect.ts` | 2 | — |
| `applyQualityRules.ts`, `ProjectSetup.ts` | 각 1 | — |

**핵심 관찰**: `src/infra/lib/figma/types.ts:34` 의 `FigmaNode` 는 **출력** 타입이고, **입력(Figma API 원본 응답)** 타입이 없어서 `extract.ts:40` `extractCSS(node: any)`, `walkNode(node: any)`, `isDesignTextNode(n: any)` 등이 전부 `any` 로 열려 있다. `FigmaApiNode` 원본 인터페이스(부분 타입으로 충분)를 `types.ts` 에 추가하면 15건 중 대부분이 한 번에 닫힌다. → **P2**

### 4.2 `console.log`

- `src/cli/` 30개 파일 — CLI 사용자 출력이므로 정당.
- `src/infra/`, `src/tools/` **9건 / 6개 파일** — 라이브러리 계층이라 의심 대상:
  `infra/lib/utils.ts`, `infra/lib/codex-proxy.ts`, `infra/lib/ReviewRace.ts`, `infra/lib/SkillRepository.ts`, `tools/index.ts`, `tools/interaction/askUser.ts`
  → CLAUDE.md 의 "No `console.log` in commits" 하드룰과 충돌. 의도된 진단 출력이면 `qualityCheck.consoleAllow` 글롭에 등록해 게이트와 정합을 맞추는 편이 낫다. → **P2**

### 4.3 함수 길이 / 복잡도

50줄 초과 함수 111개 (휴리스틱 — `switch` 블록·템플릿 생성기 포함). 실제로 문제되는 상위:

| 위치 | 길이 | 판정 |
|---|---|---|
| `src/cli/postinstall/cursor-skills.ts:13` `generateCursorSkills` | 786L | ⚠️ 실체는 인라인 템플릿 리터럴 53개 덩어리. 로직 복잡도는 낮으나 **에셋이 코드에 하드코딩**된 형태 — `skills/` 처럼 파일로 분리하면 786L → ~40L. **P2** |
| `src/cli/commands/init.ts:216` `init` | 311L | ⚠️ 실제 분해 대상. **P2** |
| `src/tools/convention/suggestImprovements.ts:27` | 263L | ⚠️ **P3** |
| `src/tools/convention/checkCouplingCohesion.ts:43` | 249L | ⚠️ **P3** |
| `src/tools/ui/previewUiAscii.ts:61` | 236L | ⚠️ **P3** |
| `src/cli/index.ts:229` `switch (command)` | 316L | 함수 아님(라우터). 케이스별 위임 구조라 허용 가능 |
| `src/infra/lib/codex-proxy.ts` (1139L 파일) | — | ✅ 최대 파일이지만 30개 이상 소형 함수 + 명시적 인터페이스로 잘 분해돼 있음. 리팩터링 대상 아님 |

### 4.4 테스트 배치

- **커버리지 설정 결함**: `vitest.config.ts:20` 의 `coverage.include` 가 `src/lib/**`, `src/tools/**` 인데 **`src/lib/` 는 존재하지 않는다**(실제 경로는 `src/infra/lib/`). 즉 커버리지 리포트가 `src/infra/` 전체를 조용히 누락한다. → **P1** (측정 자체가 틀림)
- 직접 대응 테스트가 없는 모듈 **155 / 196 (79%)**. 다만 통합 테스트(`src/__tests__/`, `hooks/scripts/__tests__/`)가 계약 수준을 덮고 있어 실제 위험은 숫자보다 낮다.
- `hooks/scripts/` 는 50개 스크립트에 테스트 23개 — 런타임 크리티컬 영역치고 밀도가 낮다. 특히 대형 스크립트 `clone-extract.js`(1291L), `clone-refine.js`(642L), `figma-to-scss.js`(394L), `context-save.js`(342L) 는 전용 테스트가 없다. → **P2**

---

## 5. 의존성

### 5.1 취약점 (`npm audit`)

| 패키지 | 심각도 | 경로 | 프로덕션 도달 |
|---|---|---|---|
| `brace-expansion` ≤5.0.7 (DoS/OOM) | **high** | `glob@13.0.6 → minimatch@10.2.5` | **예** — `glob` 은 dependencies |
| `fast-uri` 3.0.0–3.1.3 (host confusion) | high | `ajv@8.20.0` | 아니오 (devDep) |
| `postcss` ≤8.5.17 | high | `vitest → vite` | 아니오 (devDep) |
| `@hono/node-server` <2.0.5 | moderate | `@anthropic-ai/claude-agent-sdk → @modelcontextprotocol/sdk` | optionalDependency |

→ 실사용자에게 도달하는 건 **`brace-expansion` 1건**. `npm audit fix` 로 해소 가능. → **P2**

### 5.2 낙후 패키지

| 패키지 | 현재 | 최신 | 메이저 격차 |
|---|---|---|---|
| `typescript` | 5.9.3 | 7.0.2 | **2** |
| `@types/node` | 22.20.0 | 26.1.2 | **4** |
| `ts-morph` | 26.0.0 | 28.0.0 | 2 |
| `@anthropic-ai/claude-agent-sdk` | 0.2.141 | 0.3.220 | 1 (minor, 0.x) |
| `chalk` | 5.6.2 | 6.0.0 | 1 |
| `better-sqlite3` | 12.11.1 | 13.0.1 | 1 |
| `@ast-grep/napi` | 0.40.5 | 0.45.0 | — |

임계 기준(메이저 2 이상 = Critical)에 `typescript`, `@types/node`, `ts-morph` 3건이 걸린다. `@types/node` 는 `engines: node>=18` 선언과의 정합도 함께 봐야 한다. → **P2**

### 5.3 락파일 이중화

- `.gitignore:11` 이 `package-lock.json` 을 "npm - unused" 로 제외하지만, 작업 트리에는 최근 갱신된 `package-lock.json`(131KB)과 `pnpm-lock.yaml`(56KB)이 **둘 다 존재**한다.
- CI(`test.yml`, `release.yml`)는 `pnpm install --frozen-lockfile=false` 를 쓴다 — `--frozen-lockfile=false` 라서 락파일이 낡아도 조용히 통과한다. 즉 **로컬(npm)과 CI(pnpm)의 해석 트리가 갈라져도 아무도 못 잡는다.**
- 재현성 관점에서 P2: 매니저를 하나로 고정하고 CI는 frozen lockfile 로 돌리는 게 맞다. → **P2**

---

## 6. 작업 트리 상태 (참고)

커밋되지 않은 변경이 19개 파일 / +254 −112 존재하며, 신규 미추적 파일 6개가 포함된다:

- `hooks/scripts/lib/anchor.js`, `lib/console-allow.js`, `lib/inbox.js` (신규 hooks 라이브러리)
- `hooks/scripts/__tests__/anchor-inbox.test.js`, `code-check-false-positive.test.js`
- `src/__tests__/instruction-drift.test.ts`

주제는 **loop-contract 의 stuck 시맨틱 정리 + code-check 오탐 수정**으로 일관되며, 문서(SSOT)와 구현·테스트가 함께 움직이고 있다. 신규 로직에 테스트가 동반돼 있어 상태는 건강하다. 다만 미커밋 규모가 커지는 중이라 커밋 단위로 끊는 편이 좋다.

**로컬 잔여물**: `skills/` 아래 빈 디렉터리 4개 — `create-educational-content/`, `vibe.arch-guard/`, `vibe.exec-plan/`, `vibe.restraint/`. git 은 빈 디렉터리를 추적하지 않으므로 배포엔 영향 없고, 삭제된 스킬의 로컬 껍데기다. (`create-educational-content` 는 `constants.ts:317` 의 legacy→신규 rename 맵 엔트리로 의도적 잔존 — 버그 아님.) → **P3, 로컬 정리만**

---

## 7. 우선순위 조치

### P1
1. **`vitest.config.ts:20` 커버리지 경로 오류** — `src/lib/**/*.ts` → `src/infra/lib/**/*.ts`. 현재 `src/infra` 전체가 커버리지 측정에서 조용히 빠져 있어, 커버리지 수치 자체를 신뢰할 수 없다.

### P2
2. `FigmaApiNode` 원본 입력 타입을 `src/infra/lib/figma/types.ts` 에 추가 → `extract.ts`/`audit.ts` 의 `any` 15건 제거.
3. `npm audit fix` — 프로덕션 경로의 `brace-expansion` high 취약점 해소.
4. 락파일/패키지 매니저 단일화 + CI 를 frozen lockfile 로 전환.
5. `src/infra`·`src/tools` 의 `console.log` 9건 — 제거하거나 `qualityCheck.consoleAllow` 에 등록해 하드룰과 정합.
6. `cursor-skills.ts` 인라인 템플릿(786L)을 파일 에셋으로 분리; `init.ts:216` (311L) 분해.
7. `hooks/scripts/` 대형 무테스트 스크립트에 테스트 추가 — `clone-extract.js`(1291L) 우선.
8. `typescript`/`@types/node`/`ts-morph` 메이저 갱신 계획 수립.

### P3
9. `CodexProxyConfig`/`GlobalVibeConfig` 를 `src/infra/types/` 로 이동해 레이어 방향 완전 단방향화.
10. `suggestImprovements`/`checkCouplingCohesion`/`previewUiAscii` 함수 분해.
11. `skills/` 빈 디렉터리 4개 로컬 정리.

---

## 8. 품질 게이트 자기평가

| 항목 | 가중치 | 충족 |
|---|---|---|
| 관련 파일 전부 식별 | 20% | ✅ |
| 진입점/모듈 경계 문서화 | 15% | ✅ |
| 데이터/의존성 모델 매핑 | 15% | ✅ |
| 파일 경로 실재 검증 | 10% | ✅ |
| 라인 번호 정확 | 10% | ✅ |
| 비즈니스 로직 설명 | 10% | ✅ |
| 의존성 매핑 | 10% | ✅ |
| 다음 단계 명시 | 10% | ✅ |

**Score: 100/100 · Grade: EXCELLENT · Depth: L3**
