# Integrated worktree records

These records were committed and merged into main from the pending Windows fixture and discovery cohort worktrees. Their original scenario and run identities remain separate because the worktrees reused run IDs for different checks. They are historical evidence, not a new completion or execution approval.

The test concurrency setting is integrated in `vitest.config.ts`; the frozen protocol, cohort ledger, CI evidence and results are integrated under `bench/claims/4.1.26/`. `integration.json` maps each pending source file to its final location and source commit.
