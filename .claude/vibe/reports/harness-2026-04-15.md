# Harness Diagnosis — 2026-04-15

**Project type**: npm package/library (`@su-record/vibe`)
N/A items: `docs/`, `.dev/`, `.dev/learnings/`, `docs/ referenced` (denominator 100 → 86)

## Score and Grade

- **Score (final)**: **80/86 (93%)**
- **Score (initial)**: 75/86 (87%)
- **Grade**: **S** (production-ready Harness)

## Axis Scores

| Axis | Score | Details |
|------|-------|---------|
| Scaffolding | 11/13 | 역할별 src/ (cli/infra/tools), 계층 분리, .gitignore 완비. 테스트 src/ co-located (이 프로젝트의 의식적 선택). |
| Context | 15/17 | CLAUDE.md 105줄 pointer-based, 스킬 tier 분리 (core/standard/optional) 완전. `.claude/rules/` 부재는 CLAUDE.md SSOT 원칙 상의 의도적 선택. |
| Planning | 14/15 | SPEC 11 · Feature 11 (1:1), vibe-interview + 타입별 체크리스트 6종, 분산 템플릿. |
| Orchestration | 15/15 | 에이전트 46 · 스킬 48 · 팀 구성 · 권한 분리 · 비코드 워크플로 풍부. |
| Verification | **14/15** ⬆️ | build+test CI required, 테스트 **981/981 녹색**, pre/post-tool 훅, /vibe.trace·/vibe.contract·/vibe.verify. Traceability 부분 점수. |
| Compounding | 11/11 | evolution engine, MemoryStorage, 스킬/룰 축적. |

## Resolved This Session

- [x] **scope-guard 훅** — PreToolUse(Edit/Write) 선언적 스코프 강제
- [x] **scope.json 자동 동기화** — 활성 SPEC에서 allow glob 자동 파생 (SessionStart + init/update 트리거)
- [x] **CI 워크플로** `.github/workflows/test.yml` — build + test 모두 required
- [x] **270 실패 테스트 해결** — 진짜 regression 0개. 루트 원인: pnpm 10이 better-sqlite3 native build 스크립트 차단. `package.json.pnpm.onlyBuiltDependencies = ["better-sqlite3"]` 추가 + 수동 `npm run install` 후 981/981 녹색 복원.

## Deferred (의식적 선택)

- **`.claude/rules/` 분리**: CLAUDE.md가 "content SSOT"로 선언됨 → 외부화는 SSOT 원칙과 충돌. Context 축 2점 손실은 수용.
- **테스트 co-location**: 기존 패턴과 일관성. ADR로 정당화 가능.

## UI Testing 검토

OpenAI harness engineering 글에서 "Browser expert agent"의 E2E/visual regression을 핵심으로 언급. **이 프로젝트 (vibe)에는 해당 없음**:
- 산출물: CLI + 라이브러리 (자체 UI 없음)
- 이미 소비자 쪽 UI 테스트 능력 보유: `skills/e2e-*` 에이전트, `/vibe.utils` (Playwright E2E)

즉 "자기 자신에는 UI 없음 → 대상 밖", "생성물 측은 이미 지원 중".
