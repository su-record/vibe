# Design

Start from the user's task, audience, content and existing visual language. Use clear hierarchy, readable text and deliberate spacing. Preserve accessible labels, contrast, focus and failure states. Avoid decorative layouts that add no meaning.

Inspect the rendered result when changing visual behavior. Apply guidance while creating the artifact; a separate model or art director is not required by default.
