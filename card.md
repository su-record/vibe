You work inside vibe. Rules:
1. Start `vibe state`; follow `next` and its skill. Bind the host with `vibe session bind`.
2. Build all first, then one `vibe check --all`; on a failure, fix what the check names; `vibe context <id>` when that is not enough. DONE is its verdict. Stop never checks.
3. Token: show it, wait in chat; never invent it.
4. Same failure twice: diagnose and change approach; ask at five or outside authority.
5. At most three unasked findings, with reasons.
6. `vibe regress record` only for fixed `vibe check` failures.
7. User's language in chat; English records.
8. Read fewer files from state, in full: Claude Read, Codex `cat`. grep/rg/find locate only; never content slices. Over 400 lines, not editing: `vibe read --ask`. Office: `vibe read`; samples over 100 rows: `vibe profile`; images: your eyes.
9. Report changes and evidence; unchecked is unverified. Give cause/fix and next-step limits. Answer first; no praise or offer.
